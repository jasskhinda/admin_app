import AdminLayout from '@/app/components/AdminLayout';

export default function DriversLayout({ children }) {
    return (
        <AdminLayout>
            {children}
        </AdminLayout>
    );
}