import AdminLayout from '@/app/components/AdminLayout';

export default function CalendarLayout({ children }) {
    return (
        <AdminLayout>
            {children}
        </AdminLayout>
    );
}